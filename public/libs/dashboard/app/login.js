"use strict";
var $ = require('jquery');
var Login;
(function (Login_1) {
    var Login = (function () {
        function Login(form) {
            if (form === void 0) { form = $("form"); }
            this.form = form;
        }
        return Login;
    }());
})(Login || (Login = {}));
